public interface HitungBMI {
    // Definisi metode untuk menghitung BMI
    double hitungBMI(double tinggiBadan, double beratBadan);

    // Definisi metode untuk mendapatkan status BMI
    String getStatusBMI(double bmi);
}
